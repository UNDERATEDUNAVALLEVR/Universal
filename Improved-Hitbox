local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

-- Global Variables
getgenv().HitboxSize = 10
getgenv().HitboxTransparency = 0.9
getgenv().HitboxStatus = false
getgenv().TeamCheck = false

-- UI Setup via Your External Obfuscated Library
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/ujerald789/UI/refs/heads/main/UI"))()
local Window = Library:Create("Universal Hitbox | by ujerald789")

-- Main Hitbox Tab
local MainTab = Window:Tab("Hitbox", "rbxassetid://10888331510")

MainTab:Toggle("Status", function(state)
    getgenv().HitboxStatus = state
end)

MainTab:Toggle("Team Check", function(state)
    getgenv().TeamCheck = state
end)

MainTab:TextBox("Hitbox Size (Default: 10)", function(value)
    local num = tonumber(value)
    if num and num > 0 then
        getgenv().HitboxSize = num
    end
end)

MainTab:TextBox("Transparency 0-1 (Default: 0.9)", function(number)
    local num = tonumber(number)
    if num then
        getgenv().HitboxTransparency = math.clamp(num, 0, 1)
    end
end)

-- Feedback Tab
local FeedbackTab = Window:Tab("Feedback", "rbxassetid://12296135476")

FeedbackTab:Button("Open Feedback Menu", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/ujerald789/FB/main/FB"))()
end)

-- Safe Team Check Function
local function isTeammate(p1, p2)
    if not p1 or not p2 then return false end

    -- 1. Built-in Roblox Team Object Comparison
    if p1.Team ~= nil and p2.Team ~= nil then
        return p1.Team == p2.Team
    end

    -- 2. Built-in Roblox TeamColor Fallback
    if p1.TeamColor ~= nil and p2.TeamColor ~= nil then
        return p1.TeamColor == p2.TeamColor
    end

    -- 3. Custom Value Folder inside Player
    local t1 = p1:FindFirstChild("Team") or p1:FindFirstChild("TeamValue")
    local t2 = p2:FindFirstChild("Team") or p2:FindFirstChild("TeamValue")
    if t1 and t2 and t1:IsA("ValueBase") and t2:IsA("ValueBase") then
        return t1.Value == t2.Value
    end

    return false
end

-- RenderStepped Target Loop
RunService.RenderStepped:Connect(function()
    local localPlayer = Players.LocalPlayer

    for _, v in ipairs(Players:GetPlayers()) do
        if v ~= localPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = v.Character.HumanoidRootPart
            
            if getgenv().HitboxStatus then
                local shouldExpand = false

                if not getgenv().TeamCheck then
                    shouldExpand = true
                else
                    -- Expand only if they are NOT on your team
                    if not isTeammate(localPlayer, v) then
                        shouldExpand = true
                    end
                end

                if shouldExpand then
                    pcall(function()
                        hrp.Size = Vector3.new(getgenv().HitboxSize, getgenv().HitboxSize, getgenv().HitboxSize)
                        hrp.Transparency = getgenv().HitboxTransparency
                        hrp.BrickColor = BrickColor.new("Really black")
                        hrp.Material = Enum.Material.Neon
                        hrp.CanCollide = false
                    end)
                else
                    -- Teammate Reset
                    pcall(function()
                        hrp.Size = Vector3.new(2, 2, 1)
                        hrp.Transparency = 1
                        hrp.BrickColor = BrickColor.new("Medium stone grey")
                        hrp.Material = Enum.Material.Plastic
                        hrp.CanCollide = false
                    end)
                end
            else
                -- Off State Reset
                pcall(function()
                    hrp.Size = Vector3.new(2, 2, 1)
                    hrp.Transparency = 1
                    hrp.BrickColor = BrickColor.new("Medium stone grey")
                    hrp.Material = Enum.Material.Plastic
                    hrp.CanCollide = false
                end)
            end
        end
    end
end)
